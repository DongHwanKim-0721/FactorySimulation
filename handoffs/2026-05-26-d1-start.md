# Handoff — FactorySimulation 단계 D 리팩터링

## Context

`DongHwanKim-0721/FactorySimulation` repo의 **단계 D (리팩터링)** 진행 중. 1200줄 `WholeCode.py` 단일 클래스를 책임별로 분리하는 작업.

- **로컬 작업 디렉터리**: `C:\Users\pqlle\vscode-python\FactorySimulation\`
  - `PRD.md` (이미 repo에 푸시됨)
  - `WholeCode.py` (50KB, 미수정 — 단계 D 끝에 `.bak`으로 rename 예정)
- **리모트**: https://github.com/DongHwanKim-0721/FactorySimulation
  - `main` 브랜치
  - 리모트에는 별개로 `WholeCode` (확장자 없는 파일) 존재 — 정리 필요할 수 있음
- **로컬은 git repo가 아님** — `git init` 미수행. 지금까지 모든 작업은 `gh` CLI로 처리.
- **gh 인증**: `DongHwanKim-0721` 계정으로 로그인됨 (repo 소유자)

## What's already done

- PRD.md 작성 및 repo에 푸시 (커밋 `6fc11183`)
- 이슈 4개 등록:
  - **#1** 부모 이슈 (단계 D 트래킹)
  - **#2** [D1] 엔진 분리 + 골든 테스트 안전망 — AFK, 즉시 시작 가능
  - **#3** [D2] Scenario 클래스 + 시나리오 직렬화 — AFK, blocked by #2
  - **#4** [D3] GUI 재작성 + WholeCode 폐기 — HITL, blocked by #2 #3

상세 내용은 각 이슈와 [PRD.md](https://github.com/DongHwanKim-0721/FactorySimulation/blob/main/PRD.md) 참조 (중복하지 않음).

## What's next

다음 세션은 **#2 (D1) 작업 시작**부터. 사용자가 아직 작업 시작 승인을 안 했음 (마지막 메시지에 "#2부터 시작할 수 있어. 작업 시작할까?" 질문 후 handoff 요청 들어옴).

권장 첫 단계:
1. 사용자에게 D1 시작 의사 확인
2. 로컬을 git repo로 초기화할지 결정 (현재 `gh` API 단발 호출로만 처리 중)
3. 디렉터리 구조 (`engine/`, `tests/`) 생성 및 `WholeCode.py`에서 `simulate()` / `topological_flow()` 로직 이식
4. T1/T2 골든 테스트 작성 후 통과 확인
5. 일회성 레거시 검증 스크립트로 구코드 vs 신엔진 결과 일치 확인

## Constraints to honor

PRD에 명시된 것 외에 대화 중 합의된 사항:

- 엔진 이식 시 **내부 계산 로직은 한 줄도 안 건드린다** — 이름/시그니처만 다듬음
- `capacity` 무력화와 분기/합류 선형화 버그는 D 단계에서 **수정하지 않고** 현재 동작을 골든에 캡처하지도 않음 (다음 단계 A에서 처리)
- `WholeCode.py`는 D 단계 끝까지 살아있음 — 마지막 단계에서 `.bak`으로 rename
- 식별자는 영어, 사용자 보이는 라벨만 한국어
- 시나리오 파일 포맷에 버전 필드 없음 (YAGNI)
- triage 라벨 체계 없음 — 기본 GitHub 라벨만 존재 (`/setup-matt-pocock-skills`은 Out of Scope)

## Suggested skills

- **`tdd`** — D1은 골든 테스트(T1=300, T2=1275) 먼저 작성 → 엔진 이식 → 통과 확인의 TDD 흐름 (PRD에서 명시)
- **`verify`** — D3 종료 시 수동 스모크 테스트 (블록 추가/이동/연결/삭제/시뮬레이션/저장/불러오기/Free Block 이름) 수행
- **`run`** — GUI 재작성 후 앱 실제 실행 확인용
- `diagnose` — 레거시 검증 스크립트가 불일치 보고하면 사용

## Open questions for next session

- 로컬 `FactorySimulation/` 폴더를 `git init`해서 일반 git 워크플로우로 전환할지, 계속 `gh` API로 단발 푸시할지
- 리모트의 `WholeCode` (확장자 없음) 파일은 어떻게 정리할지 (로컬 `WholeCode.py`와 중복/충돌)
- 단계 D 종료 후 단계 A(엔진 교체) PRD 작성 시점
